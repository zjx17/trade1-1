<div>
  <div class="pull-right">
    <%= link_to "新建发布", new_product_publish_path, class: "btn btn-primary new-address-btn" %>
    <!-- <%= link_to "新建发布", new_admin_product_path, class: "btn btn-primary" %> -->
  </div>

  <h1>
    我的发布(<%= @products.total_entries %>)
  </h1>
</div>

<div>
  <table class="table table-striped">
    <tr>
      <th>ID</th>
      <th>标题</th>
      <th>发布人</th>
    </tr>

    <% @products.each do |product| %>
      <tr>
        <td><%= product.id %></td>
        <td><%= product.title %></td>
        <td><%= current_user.username %></td>
        <td align="right">
          <%= link_to "编辑", edit_product_publish_path(product) %> 
          <%= link_to "删除", product_publish_path(product), method: :delete, 'data-confirm': "确认删除吗?" %>
          <%= link_to "图片管理", product_publish_publish_images_path(publish_id: product) %>
        </td>
      </tr>
    <% end -%>
  </table>

  <%= will_paginate @products %>
</div>
